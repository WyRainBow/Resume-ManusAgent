# 动态提示词机制

**文档版本**: 1.0
**最后更新**: 2026-01-06

## 📋 概述

OpenManus 采用**动态提示词**机制，根据对话状态、用户意图和上下文信息实时生成提示词，确保 LLM 能够获得最相关的指导信息。

**核心设计理念**:
- **状态感知**: 根据对话状态（简历是否加载、当前文件路径等）动态调整提示词
- **上下文注入**: 将关键上下文信息注入到系统提示词中
- **场景适配**: 根据不同场景（分析完成、普通对话等）生成不同的下一步提示词
- **避免重复**: 智能判断是否需要添加提示词，避免消息膨胀

---

## 🏗️ 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    Manus.think()                            │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  1. 获取用户输入                                        │  │
│  │  2. 意图识别                                            │  │
│  │  3. 生成动态提示词                                      │  │
│  │     ├─ _generate_dynamic_prompts()                     │  │
│  │     │   ├─ 收集上下文信息                              │  │
│  │     │   ├─ 生成系统提示词 (注入变量)                   │  │
│  │     │   └─ 生成下一步提示词                            │  │
│  │     └─ _generate_next_step_prompt()                    │  │
│  │         ├─ 根据意图判断                                │  │
│  │         ├─ 检查分析工具执行状态                        │  │
│  │         └─ 生成场景化提示词                            │  │
│  │  4. 调用 LLM (传入动态提示词)                         │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 实现细节

### 1. 动态系统提示词生成

**位置**: `app/agent/manus.py` → `_generate_dynamic_prompts()`

**流程**:
```python
async def _generate_dynamic_prompts(self, user_input: str, intent: "Intent" = None) -> tuple:
    """根据用户输入和对话状态动态生成提示词"""

    # 1. 收集上下文信息
    context_parts = []
    if self._conversation_state.context.resume_loaded:
        context_parts.append("✅ 简历已加载")
    else:
        context_parts.append("⚠️ 简历未加载，建议先加载简历")

    if self._current_resume_path:
        context_parts.append(f"📄 当前简历文件: {self._current_resume_path}")

    context = "\n".join(context_parts) if context_parts else "初始状态"

    # 2. 生成系统提示词（注入动态变量）
    system_prompt = SYSTEM_PROMPT.format(
        directory=config.workspace_root,
        context=context
    )

    # 3. 生成下一步提示词
    next_step = await self._generate_next_step_prompt(intent)

    return system_prompt, next_step
```

**关键点**:
- 根据 `resume_loaded` 状态生成不同的上下文描述
- 如果有当前简历路径，添加到上下文中
- 使用 `.format()` 方法注入变量到系统提示词模板

### 2. 系统提示词模板

**位置**: `app/prompt/manus.py` → `SYSTEM_PROMPT`

**模板结构**:
```python
SYSTEM_PROMPT = """You are OpenManus, an AI assistant for resume optimization.

## Core Principles
1. **Resume-related tasks** → Use appropriate tools
2. **General questions** → Answer directly using your knowledge, NO tools
3. **Understand context** → Consider conversation history and resume state

## Available Tools
| Tool | When to Use |
|------|-------------|
| cv_reader_agent | Load resume from file path |
| cv_analyzer_agent | Analyze resume quality and content |
| education_analyzer | Analyze education background specifically |
| cv_editor_agent | Edit resume content |
| terminate | Complete the task |

## Guidelines
- **DO** use tools for resume operations (loading, analyzing, editing)
- **DO NOT** use browser/search tools for general knowledge questions
- **DO** answer common questions directly using your own knowledge
- **DO** call terminate when the task is complete
- Working language: Chinese

Current directory: {directory}
Current state: {context}
"""
```

**动态变量**:
- `{directory}`: 工作目录路径（从 `config.workspace_root` 获取）
- `{context}`: 当前对话状态（动态生成）

**示例输出**:
```
Current directory: /Users/wy770/Resume_OpenMauns/OpenManus
Current state: ✅ 简历已加载
📄 当前简历文件: /app/docs/韦宇_简历.md
```

### 3. 动态下一步提示词生成

**位置**: `app/agent/manus.py` → `_generate_next_step_prompt()`

**决策逻辑**:
```python
async def _generate_next_step_prompt(self, intent: "Intent" = None) -> str:
    """生成下一步提示词"""

    # 1. 对话类意图：返回空字符串，让 LLM 自然回答
    if intent in [Intent.GREETING, Intent.UNKNOWN]:
        return ""  # 避免重复消息

    # 2. 检查是否有分析工具刚执行完
    recent_analysis = False
    analysis_tool_name = None

    # 检查最近的消息中是否有分析工具调用
    for msg in reversed(self.memory.messages[-3:]):
        if hasattr(msg, 'tool_calls') and msg.tool_calls:
            for tc in msg.tool_calls:
                if tc.function.name in ['education_analyzer', 'cv_analyzer_agent']:
                    recent_analysis = True
                    break

    if not recent_analysis:
        return NEXT_STEP_PROMPT  # 默认空字符串（已简化）

    # 3. 检查分析结果是否已返回
    analysis_result_returned = False
    for msg in reversed(self.memory.messages[-5:]):
        if msg.role == "tool" and msg.name in ['education_analyzer', 'cv_analyzer_agent']:
            analysis_result_returned = True
            analysis_tool_name = msg.name
            break

    if not analysis_result_returned:
        return NEXT_STEP_PROMPT

    # 4. 获取分析结果内容
    analysis_content = ""
    for msg in reversed(self.memory.messages[-10:]):
        if msg.role == "tool" and msg.name in ['education_analyzer', 'cv_analyzer_agent']:
            analysis_content = msg.content[:5000]
            break

    # 5. 生成场景化提示词
    tool_display_name = "教育经历" if analysis_tool_name == "education_analyzer" else "简历"
    return f"""## 分析完成，请展示结果

分析工具 ({analysis_tool_name}) 已返回结果，请向用户展示：

{analysis_content[:2000]}

请用中文向用户展示分析结果摘要和优化建议，然后询问是否要应用优化。"""
```

**关键逻辑**:
1. **对话类意图**: 返回空字符串，让 LLM 自然回答（避免重复消息）
2. **分析工具执行后**: 检查是否有分析工具刚执行完
3. **结果展示**: 如果分析结果已返回，生成结果展示提示词
4. **内容截取**: 只取前 2000 字符，避免提示词过长

---

## 📊 动态提示词示例

### 示例 1: 简历未加载状态

**上下文信息**:
- `resume_loaded = False`
- `_current_resume_path = None`

**生成的系统提示词**:
```
You are OpenManus, an AI assistant for resume optimization.

## Core Principles
...

Current directory: /Users/wy770/Resume_OpenMauns/OpenManus
Current state: ⚠️ 简历未加载，建议先加载简历
```

**下一步提示词**: `""` (空字符串，让 LLM 自然回答)

---

### 示例 2: 简历已加载状态

**上下文信息**:
- `resume_loaded = True`
- `_current_resume_path = "/app/docs/韦宇_简历.md"`

**生成的系统提示词**:
```
You are OpenManus, an AI assistant for resume optimization.

## Core Principles
...

Current directory: /Users/wy770/Resume_OpenMauns/OpenManus
Current state: ✅ 简历已加载
📄 当前简历文件: /app/docs/韦宇_简历.md
```

**下一步提示词**: `""` (空字符串)

---

### 示例 3: 分析工具执行完成后

**上下文信息**:
- `resume_loaded = True`
- 最近有 `cv_analyzer_agent` 工具调用
- 工具结果已返回

**生成的系统提示词**:
```
Current directory: /Users/wy770/Resume_OpenMauns/OpenManus
Current state: ✅ 简历已加载
📄 当前简历文件: /app/docs/韦宇_简历.md
```

**下一步提示词**:
```
## 分析完成，请展示结果

分析工具 (cv_analyzer_agent) 已返回结果，请向用户展示：

[分析结果内容前 2000 字符...]

请用中文向用户展示分析结果摘要和优化建议，然后询问是否要应用优化。
```

---

## 🎯 动态提示词的优势

### 1. 状态感知
- 根据对话状态动态调整提示词
- LLM 能够了解当前上下文（简历是否加载、当前文件等）

### 2. 场景适配
- 不同场景生成不同的提示词
- 分析完成后自动生成结果展示提示词

### 3. 避免重复
- 对话类意图返回空字符串，避免重复消息
- 智能判断是否需要添加提示词

### 4. 可扩展性
- 可以轻松添加新的上下文信息
- 可以添加新的场景化提示词模板

---

## 🔄 提示词生成流程

### 完整流程

```
用户输入
    │
    ▼
意图识别
    │
    ▼
_generate_dynamic_prompts()
    │
    ├─ 收集上下文信息
    │   ├─ resume_loaded?
    │   ├─ _current_resume_path?
    │   └─ 其他状态信息
    │
    ├─ 生成系统提示词
    │   └─ SYSTEM_PROMPT.format(
    │       directory=config.workspace_root,
    │       context=context
    │   )
    │
    └─ 生成下一步提示词
        └─ _generate_next_step_prompt(intent)
            ├─ 对话类意图 → ""
            ├─ 分析工具执行后 → 结果展示提示词
            └─ 其他情况 → NEXT_STEP_PROMPT
    │
    ▼
调用 LLM (传入动态提示词)
    │
    ▼
LLM 根据提示词和工具描述决策
```

---

## 📝 代码位置总结

| 功能 | 文件 | 方法 |
|------|------|------|
| 动态提示词生成 | `app/agent/manus.py` | `_generate_dynamic_prompts()` |
| 下一步提示词生成 | `app/agent/manus.py` | `_generate_next_step_prompt()` |
| 系统提示词模板 | `app/prompt/manus.py` | `SYSTEM_PROMPT` |
| 浏览器上下文提示词 | `app/agent/browser.py` | `format_next_step_prompt()` |
| 提示词使用 | `app/agent/toolcall.py` | `think()` |

### 浏览器上下文提示词

当检测到浏览器工具正在使用时，系统会自动添加浏览器上下文信息：

```python
# 检查是否需要浏览器上下文
recent_messages = self.memory.messages[-3:] if self.memory.messages else []
browser_in_use = any(
    tc.function.name == BrowserUseTool().name
    for msg in recent_messages
    if msg.tool_calls
    for tc in msg.tool_calls
)

if browser_in_use:
    browser_prompt = await self.browser_context_helper.format_next_step_prompt()
    if self.next_step_prompt:
        self.next_step_prompt = f"{self.next_step_prompt}\n\n{browser_prompt}"
    else:
        self.next_step_prompt = browser_prompt
```

**浏览器上下文包含**:
- 当前 URL
- 页面标题
- 可用标签页数量
- 页面滚动位置（上方/下方像素数）
- 当前页面截图（base64 编码）

---

## 🎨 设计模式

### 模板方法模式
- 系统提示词使用模板 + 变量注入
- 保持核心逻辑不变，只改变上下文信息

### 策略模式
- 根据不同意图和状态选择不同的提示词生成策略
- `_generate_next_step_prompt()` 根据意图选择策略

### 状态模式
- 根据对话状态（简历是否加载等）生成不同的提示词
- 状态变化时提示词自动更新

---

## 💡 最佳实践

### 1. 上下文信息收集
- 只收集必要的上下文信息
- 避免信息过载

### 2. 提示词长度控制
- 分析结果只取前 2000 字符
- 避免提示词过长导致 token 消耗过大

### 3. 避免重复
- 对话类意图返回空字符串
- 智能判断是否需要添加提示词

### 4. 场景适配
- 不同场景使用不同的提示词模板
- 保持提示词的针对性和有效性

---

## 🔮 未来优化方向

1. **更多上下文信息**: 可以添加更多上下文信息（如最近使用的工具、对话摘要等）
2. **提示词缓存**: 对于相同状态的提示词可以缓存
3. **提示词版本管理**: 可以管理不同版本的提示词模板
4. **A/B 测试**: 可以测试不同提示词的效果

---

## ✅ 总结

动态提示词机制的核心特点：

1. **状态感知**: 根据对话状态动态调整提示词
2. **上下文注入**: 将关键信息注入到系统提示词中
3. **场景适配**: 不同场景生成不同的提示词
4. **避免重复**: 智能判断是否需要添加提示词
5. **可扩展性**: 易于添加新的上下文信息和场景

这种设计确保了 LLM 能够获得最相关的指导信息，同时保持了代码的简洁性和可维护性。

